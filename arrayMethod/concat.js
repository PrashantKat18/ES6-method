// concat( )
// This method is used to merge two or more arrays and returns a
// new array, without changing the existing arrays.

const arr = [1,2,3,4,5];
const arr1 = [7,8,9,10];
const arr2 = [17,28,19,20];
let merged = arr.concat(arr1,arr2);
console.log(merged);